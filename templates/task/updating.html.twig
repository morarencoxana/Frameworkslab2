{% extends 'base.html.twig' %}

{% block title %}Hello TaskController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{# Апдейт наших задач #}

<p align=center><b>Обновить задачу</b></p>

<form action="{{ path('app_task_update', { id: task.id }) }}" method="post" style="margin-left: 110px;">
    {{ form_start(task_form) }}
    {{ form_row(task_form.name, { required: true }) }}
    {{ form_row(task_form.description, { required: true, placeholder: '(optional)' }) }}
    {{ form_row(task_form.result, { required: false, placeholder: '(optional)' }) }}
    {{ form_row(task_form.date) }}
    {{ form_row(task_form.category) }}

    <button type="submit" class="btn btn-primary">Обновить</button>

    {# Кнопка возвращения на главную страницу #}

    <a href="/task/" class="btn btn-primary">Назад</a>

    {# Кнопка возвращения на главную страницу #}

{# Апдейт наших задач #}

<div class="example-wrapper">
    
</div>
{% endblock %}